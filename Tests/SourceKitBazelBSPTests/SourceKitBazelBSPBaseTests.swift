import Testing

// To be removed, just to get a working test target while we configure everything else.
@Suite("Foo suite") struct SourceKitBazelBSPBaseTests {
    @Test("Foo") func testExample() throws {
        #expect(true)
    }
}
